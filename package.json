{
  "name": "fava-portfolio-returns",
  "license": "GPL-2.0",
  "source": [
    "frontend/PortfolioReturns.html"
  ],
  "targets": {
    "default": {
      "distDir": "fava_portfolio_returns/templates"
    }
  },
  "parcelIgnore": [
    "{{.*}}",
    "does_not_exist"
  ],
  "scripts": {
    "build": "parcel build",
    "watch": "parcel watch",
    "format": "prettier -w frontend"
  },
  "devDependencies": {
    "buffer": "^5.5.0",
    "parcel": "^2.3.1",
    "parcel-resolver-ignore": "^2.0.0",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "echarts": "git+https://github.com/andreasgerstmayr/echarts.git#support-negative-log-axis-dist"
  }
}
