{
    "name": "fava-portfolio-returns",
    "scripts": {
        "build": "esbuild src/extension.ts --bundle --format=esm --outfile=../src/fava_portfolio_returns/FavaPortfolioReturns.js --minify",
        "watch": "esbuild src/extension.ts --bundle --format=esm --outfile=../src/fava_portfolio_returns/FavaPortfolioReturns.js --watch",
        "test": "jest"
    },
    "devDependencies": {
        "esbuild": "^0.21.5",
        "jest": "^29.6.1",
        "jest-image-snapshot": "^6.2.0",
        "jest-puppeteer": "^10.0.1",
        "jest-serializer-html": "^7.1.0",
        "prettier": "^3.3.2",
        "puppeteer": "^22.11.1",
        "typescript": "^5.4.2"
    },
    "dependencies": {
        "echarts": "git+https://github.com/andreasgerstmayr/echarts.git#support-negative-log-axis-dist"
    }
}
